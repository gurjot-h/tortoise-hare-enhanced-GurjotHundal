<!-- tortoise vs hare race  -->


1. I implemented a feature that displays the total wins for both tortoise and hare above the track.
I started by adding the variables that defined teh variables carrying the wins, after that i proceeded to create a function that would
display the wins only after "showresults()" and i called the function twice. Once below all the functions and once inside "showResults".



2. what was the most difficul bug or issue?

For me i found it tricky to actuaslly get the score to display the actual resluts after the race. Specifically once one of the animals reached line 70. I couldn't get it to display so i figured i should be adding one after each win eg: "tortoiseWins++" and 
"hareWins++". I also realised i wasn't calling the function once more outside the function that would actually activate the function.


##Best commit messages:

1. "set up scoardboard render score function"
2. "set up scoreboard in html structure"
3. "fix wins display format in html structure"


the pull request screenshot: 

![alt text](<Screenshot 2026-03-09 at 3.14.50 PM.png>)
